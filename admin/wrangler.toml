name = "reganchor-admin"
compatibility_date = "2026-09-07"
workers_dev = true

[assets]
directory = "./dist"
not_found_handling = "single-page-application"

# Custom domain (Cloudflare zone for reganchor.com). Safe to comment out until DNS is ready.
[[routes]]
pattern = "admin.reganchor.com"
custom_domain = true
